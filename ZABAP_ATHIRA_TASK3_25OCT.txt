*&---------------------------------------------------------------------*
*& Report ZABAP_ATHIRA_TASK3_25OCT
*&---------------------------------------------------------------------*
*&
*&---------------------------------------------------------------------*
REPORT ZABAP_ATHIRA_TASK3_25OCT.

*TYPES: BEGIN  OF ty_vbap,
*         vbeln  TYPE vbap-vbeln,
*         posnr  TYPE vbap-posnr,
*         pstyv  TYPE vbap-pstyv,
*         arktx  TYPE vbap-arktx,
*         kwmeng TYPE vbap-kwmeng,
*         zieme  TYPE vbap-zieme,
*         netwr  TYPE vbap-netwr,
*       END  OF ty_vbap.
*TYPES :BEGIN OF ty_vbak,
*         vbeln TYPE vbak-vbeln,
*         audat TYPE vbak-audat,
*         netwr TYPE vbak-netwr,
*         kunnr TYPE vbak-kunnr,
*       END OF ty_vbak.
*TYPES : BEGIN OF ty_t006a,
*          msehi TYPE t006a-msehi,
*          msehl TYPE t006a-msehl,
*        END OF ty_t006a.
*TYPES : BEGIN OF ty_kna1 ,
*          kunnr TYPE kna1-kunnr,
*          name1 TYPE kna1-name1,
*        END OF ty_kna1.
*DATA : it_vbap    TYPE TABLE OF ty_vbap,
*       wa_vbap    LIKE LINE OF it_vbap,
*       it_vbak    TYPE TABLE OF ty_vbak,
*       wa_vbak    LIKE LINE OF it_vbak,
*       it_t006a   TYPE TABLE OF ty_t006a,
*       wa_t006a   LIKE LINE OF it_t006a,
*
*       it_kna1    TYPE TABLE OF ty_kna1,
*       wa_kna1    LIKE LINE OF it_kna1,
*       prevbeln   TYPE vbap-vbeln,
*       tot_qty    TYPE  vbap-kwmeng,
*       tot_netval TYPE vbap-netwr.
*
*SELECT-OPTIONS s_num FOR wa_vbak-vbeln.
*
* START-OF-SELECTION.
*    SELECT  vbeln audat netwr kunnr FROM vbak INTO TABLE it_vbak WHERE vbeln IN s_num.
*  END-OF-SELECTION.
*
*SELECT  kunnr name1 FROM kna1 INTO TABLE it_kna1 FOR ALL ENTRIES IN it_vbak WHERE kunnr EQ it_vbak-kunnr.
*SELECT  vbeln posnr pstyv arktx kwmeng zieme netwr FROM vbap INTO TABLE it_vbap WHERE vbeln IN s_num.
*SELECT msehi msehl FROM t006a INTO TABLE it_t006a FOR ALL ENTRIES IN it_vbap WHERE spras EQ sy-langu AND msehi EQ it_vbap-zieme.
*
*DATA lv_tabix LIKE sy-tabix.
*DATA wa_vbap_temp LIKE wa_vbap.
*
*AT LINE-SELECTION.
*  HIDE : 'DETAILS'.
*  GET CURSOR VALUE v_matnr.
*  IF v_matnr = 'DOCUMENT_NUMBER_FIELD_NAME'. " Replace 'DOCUMENT_NUMBER_FIELD_NAME' with the actual field name for the document number
*    SHOW : 'DETAILS'.
*  ENDIF.
*ENDAT.
*
*LOOP AT it_vbap INTO wa_vbap.
*  wa_vbap_temp = wa_vbap.
*  lv_tabix = sy-tabix.
*  READ TABLE it_vbak INTO wa_vbak WITH  KEY vbeln = wa_vbap-vbeln.
*  AT NEW vbeln.
*    READ TABLE it_vbak INTO wa_vbak WITH  KEY vbeln = wa_vbap-vbeln.
*    READ TABLE it_kna1 INTO wa_kna1 WITH KEY kunnr = wa_vbak-kunnr.
*
*    WRITE :/ 'Document Number:', wa_vbak-vbeln,/
*           'Doc Date:', wa_vbak-audat,/
*           'Net Val:', wa_vbak-netwr,/
*           'Sold To Party:', wa_kna1-name1.
*    HIDE : 'DETAILS'.
*  ENDAT.
*
*  AT LINE-SELECTION.
*    GET CURSOR VALUE v_matnr.
*    IF v_matnr = 'DOCUMENT_NUMBER_FIELD_NAME'. " Replace 'DOCUMENT_NUMBER_FIELD_NAME' with the actual field name for the document number
*      SHOW : 'DETAILS'.
*    ENDIF.
*  ENDAT.
*
*  READ TABLE it_t006a INTO wa_t006a WITH KEY msehi = wa_vbap-zieme.
*
*  WRITE : / wa_vbap-posnr,
*           20 wa_vbap-pstyv,
*           35 wa_vbap-arktx,
*           50 wa_vbap-kwmeng,
*           80 wa_t006a-msehl,
*           90 wa_vbap-netwr.
*
*  AT END OF vbeln.
*    SUM.
*    WRITE : /'sum:', 50 wa_vbap-kwmeng, 90 wa_vbap-netwr.
*  ENDAT.
*ENDLOOP.

TYPES : BEGIN OF ty_vbap,
     vbeln type vbap-vbeln,
     posnr TYPE vbap-posnr,
     pstyv TYPE vbap-pstyv,
     arktx TYPE vbap-arktx,
     kwmeng TYPE vbap-kwmeng,
     zieme TYPE vbap-zieme,
     netwr TYPE vbap-netwr,
END  OF ty_vbap.

TYPES :BEGIN OF ty_vbak,
       vbeln TYPE vbak-vbeln,
       audat TYPE vbak-audat,
       netwr TYPE vbak-netwr,
       kunnr type vbak-kunnr,
END OF ty_vbak.

TYPES : BEGIN OF ty_t006a,
       msehi type t006a-msehi,
       msehl type t006a-msehl,
END OF ty_t006a.

 TYPES : BEGIN OF ty_kna1 ,
    kunnr type kna1-kunnr,
    name1 type kna1-name1,
 END OF ty_kna1.

 DATA : it_vbap TYPE table of ty_vbap,
        wa_vbap LIKE LINE OF it_vbap,

        it_vbak TYPE table of ty_vbak,
        wa_vbak LIKE LINE OF it_vbak,

        it_t006a TYPE TABLE OF ty_t006a,
        wa_t006a like line of it_t006a,

        it_kna1 TYPE TABLE OF ty_kna1,
        wa_kna1 LIKE LINE OF it_kna1,

        prevbeln TYPE vbap-vbeln,
        t_order TYPE kwmeng,
        t_net TYPE netwr.

        t_order = 0.
        t_net = 0.

SELECT-OPTIONS s_num FOR wa_vbak-vbeln.
*
START-OF-SELECTION.
SELECT  vbeln audat netwr kunnr FROM vbak INTO TABLE it_vbak WHERE vbeln IN s_num.
SELECT  kunnr name1 FROM kna1 INTO TABLE it_kna1 FOR ALL ENTRIES IN it_vbak WHERE kunnr EQ it_vbak-kunnr.
SELECT  vbeln posnr pstyv arktx kwmeng zieme netwr FROM vbap INTO TABLE it_vbap WHERE vbeln in s_num.
SELECT msehi msehl FROM t006a INTO TABLE it_t006a FOR ALL ENTRIES IN it_vbap where spras Eq sy-langu AND msehi EQ it_vbap-zieme.
  WRITE :/ 'Document Number',
       25 'Document Date',
       45 'Net Val',
       60 'Sold To party'.
  End-of-SELECTION.

  LOOP at it_vbap INTO wa_vbap.
at new vbeln.
    READ TABLE it_vbak INTO wa_vbak WITH  KEY vbeln = wa_vbap-vbeln.
READ TABLE it_kna1 INTO wa_kna1 WITH KEY kunnr = wa_vbak-kunnr.

WRITE : / wa_vbak-vbeln,
        25 wa_vbak-audat,
         30 wa_vbak-netwr,
        60  wa_kna1-name1.
ENDAT.
ENDLOOP.
AT LINE-SELECTION.
  GET CURSOR VALUE wa_vbak-vbeln.
  SET PARAMETER ID 'MAT' FIELD wa_vbak-vbeln.
*    CALL TRANSACTION 'MM03'.
*PERFORM purchase.
  PERFORM details USING wa_vbap-vbeln.

FORM details USING p_vbeln TYPE vbap-vbeln.

WRITE :/ 'Item',
       20 'Material',
       35 'ItemDesc',
60 'Order Qty' ,
80 'UoM',
100'NetVal'.
*   LOOP at it_vbap INTO wa_vbap.
LOOP AT it_vbap INTO wa_vbap WHERE vbeln = p_vbeln.

   READ TABLE it_t006a INTO wa_t006a WITH KEY msehi = wa_vbap-zieme.
   WRITE : / wa_vbap-posnr,
            20 wa_vbap-pstyv,
            35  wa_vbap-arktx,
            50 wa_vbap-kwmeng,
            80 wa_t006a-msehl,
            90 wa_vbap-netwr.
   At END OF vbeln.
     sum.
       WRITE :/'Total',
               50 wa_vbap-kwmeng,
               90 wa_vbap-netwr.
   ENDAT.

prevbeln = wa_vbap-vbeln.
  ENDLOOP.
  ENDFORM.